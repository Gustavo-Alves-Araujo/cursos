'use client';

// Como agora todos os usuários vêm com senha padrão "123123",
// este componente não é mais necessário
// Mantido apenas para compatibilidade, mas não faz nada

export function FirstLoginRedirect() {
  // Componente vazio - não há mais redirecionamentos baseados em needs_password_reset
  return null;
}
